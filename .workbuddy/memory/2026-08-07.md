# 2026-08-07 工作日志

## 双 AI 协同开发项目 — 骨架搭建完成

- **背景**:用户希望用 WorkBuddy + Vibe Coding 工具(自然语言编程方式,如 Cursor 等)协同开发 Web 应用,采用「共享 Git 仓库 + 共识文件」协作模式。
- **技术栈**(用户确认):Next.js 14 + React 18 + TypeScript(前端,归 Vibe Coding 工具)/ Node.js + Express(后端,归 WorkBuddy),npm workspaces monorepo。
- **已完成**:
  - 目录结构:`apps/web`、`apps/api`、`docs/`
  - 核心共识文件 `AGENTS.md`(目录所有权、铁律、分支/PR 流程、代码规范)
  - `TASKS.md` 任务看板(标注 [WB]/[VC]/[BOTH] 负责人)
  - `docs/API.md` 接口契约(统一 `{code, message, data}` 响应结构 + 健康检查接口)
  - 根 `package.json`(workspaces)、`apps/web`/`apps/api` 最小骨架、Express 示例服务(含健康检查)
  - Git 仓库已初始化并提交(commit `64774a2`)
- **后续待办**:用户需推送远程仓库、clone 到 Vibe Coding 工具,并让 VC 工具初始化 Next.js 项目(`create-next-app` + `/api` 代理)。
- **后端已跑通**:`npm install --workspace=apps/api` 安装依赖成功(0 漏洞);`node server.js` 后台运行于 :4000(task i4kzAJ),`GET /api/health` 返回 200 `{"code":0,"message":"ok","data":{"status":"ok",...}}`。

## 项目定型:TMUA/ESAT 在线刷题系统 + M1 后端完成(commit 16f970a)

- **业务**:学生刷题(练习/模考)+ 维护考试数据(成绩/错题/掌握度)+ 老师维护题库。题库来源:官方真题(UAT-UK esat-tmua.ac.uk,PDF 需结构化录入,M4 处理)。
- **架构**:docs/ARCHITECTURE.md 落档。Next.js(前端,VC 负责)+ Express + Prisma(后端,WB 负责);SQLite 开发 / PostgreSQL 生产(本机无 PG/Docker,选 SQLite 起步)。
- **数据模型**:apps/api/prisma/schema.prisma — User/Question/Paper/Session/AnswerRecord/WrongBook。注意:**SQLite 不支持 Prisma enum**,已用 String + 注释合法值。
- **后端模块化**(apps/api/src/):routes/auth、questions、sessions、me;lib/grading(判分:答对+1 答错 0,数值题±0.01 容差、多选集合比较);middleware/auth(JWT+角色);统一 {code,message,data} 响应。
- **验证通过**(curl 端到端):注册/登录、题库筛选分页(学生无答案字段)、创建会话随机组卷、作答实时保存、提交判分(答对 1 分/答错进错题本)、成绩历史、掌握度统计、学生越权创建题目 403。
- **种子数据**:scripts/seed.js — 演示账号 stu@example.com / teacher@example.com(密码 123456)+ 6 道示例题(机构自编,风格参考官方,勿冒充官方真题)。
- **技术坑**:中文 query 参数需 URL 编码(测试时 topic=函数 未编码导致解析失败)。
- **下一步**:推远程仓库;VC 工具初始化 Next.js 前端(登录/刷题/题库管理页);官方真题 PDF 结构化录入(M4)。
