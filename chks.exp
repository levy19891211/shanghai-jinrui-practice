#!/usr/bin/expect -f
set timeout 90
spawn ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null root@8.219.151.140
expect {
    -re "password:" { send "Xhx19891211\r"; exp_continue }
    -re "\\$|#" { }
    timeout { puts "TIMEOUT"; exit 1 }
}
expect -re "\\$|$"
send -- {cd /root/shanghai-jinrui-practice/apps/api && node -e "const{PrismaClient}=require('@prisma/client');const p=new PrismaClient();p.session.findMany({where:{assignmentId:{not:null},submittedAt:null},select:{id:true,assignmentId:true,mode:true,studentId:true}}).then(r=>{console.log('ASSIGN_INPROGRESS',JSON.stringify(r.slice(0,5)));return p.\$disconnect()}).catch(e=>{console.log('ERR',e.message);process.exit(0)})"}
send "\r"
expect -re "\\$|$"
send "exit\r"
expect eof
